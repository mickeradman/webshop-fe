export type Theme = {
  color: {
    bgPrimary: string;
    bgSecondary: string;
    buttonBg: string;
    headerFooterBg: string;
    navLinkActive: string;
    navLinkHover: string;
    standardDelimiter: string;
    textPrimary: string;
    textSecondary: string;
  };
};

export type Sizes = {
  size: {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
  };
};
